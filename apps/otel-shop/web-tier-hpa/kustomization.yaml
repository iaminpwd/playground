# web-tier HPA 리소스 묶음
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # frontend, frontend-proxy HPA 정의
  - hpa.yaml

# 웹 티어 리소스 공통 라벨 (모니터링 및 필터링 용도)
commonLabels:
  app-tier: web
