apiVersion: v1
kind: Service
metadata:
  name: service-llm-serving
  namespace: llmops
spec:
  ports:
  - port: 11434
    targetPort: 11434
    protocol: TCP
    name: http
  selector:
    app: llm-serving
  type: ClusterIP