# node-cleanup CronJob이 kubectl 명령을 실행할 때 사용하는 서비스 계정
# 이 계정에 ClusterRole(node-cleanup)이 바인딩되어 노드 조회/삭제 권한을 가집니다.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: node-cleanup
  namespace: node-cleanup